---
id: em-justify-list
title: em-justify-list
sidebar_label: em-justify-list
slug: /plugins/em-justify-list
---

### 名称
```conf
name: em-justify-list
aliases: ['emlist']
```

### 参数

| 名称  | 类型   | description                              |
| ----- | ------ | ---------------------------------------- |
| value | number | 方向(top/right/bottom/left; 默认: right) |

> 一种 em-justify-list 的列表布局。

### 原理
1. `container` 自动适应外层的宽度，高度
2. `emlist` 层， `margin: -0.5em;`
3. `item` 层，`margin: 0.5em;`

### 用法

```jsx
// Container>emlist>item
<View w={900} auto debug="@2">
  <View plugin="emlist:6">
    <View bg="#eee" h={100} className="is-item">
      item1
    </View>
    {/* .... */}
  </View>
</View>
```

### 演示

```tsx live
<View w={900} auto debug="@2">
  <View plugin="emlist:5">
    <View bg="#eee" h={100} className="is-item">
      item1
    </View>
    <View bg="#f30" h={100} className="is-item">
      item2
    </View>
    <View bg="#ddd" h={100} className="is-item">
      item3
    </View>
    <View bg="#030" h={100} className="is-item">
      item2
    </View>
    <View bg="#ddd" h={100} className="is-item">
      item3
    </View>
    <View bg="#ccc" h={100} className="is-item">
      item3
    </View>
    <View bg="#999" h={100} className="is-item">
      item3
    </View>
  </View>
</View>
```